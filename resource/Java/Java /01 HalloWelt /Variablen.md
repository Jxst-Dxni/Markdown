```java
# variablen 
/**
* Hallo 
*/
public class Hallo 

    public static void main(string()arges) (
        system.out.println("test");
    )

    {

            System.out.println(x:HalloWelt")

            //variablen deklarieren
            //einfacher Datentyp
            float gewicht;Kommazahl mit 32bit 
            double laenge; //Kommazahl mit 64bit 
            int anzahl; 
            char mienZeichen; //einzelnes zeichen 
            boolean istEsKalt; // darf nur true(1) oder false(0) enthalten

            //komplexer datentyp
            String name;

            //Initialisierung 
            gewicht = 73.9f; 
            laenge = 3.54; 
            anzahl = 3;
            meinZeichen = 'a';
            Isteskalt = true 
            name = "Maier"; 

            //ausgeben einer Variablen 
            System.out.println(gewicht);
            System.out.println(laenge);
            System.out.println(anzahl);
            System.out.println(meinzeichen)
            System.out.println(istesKalt)
            System.out.println(name) 

            //änern einer Variablen
       
    }...
    

   
```